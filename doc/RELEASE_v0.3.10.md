# SaveandLoad2 v0.3.10 (Beta)

> 发布日期：2026-09-06
> 类型：测试版（Beta）
> 上一版本：[v0.3.9](./RELEASE_v0.3.9.md)

本版本新增**一键导出单局对局数据**和**卡牌附魔解析**两个功能，并修复了毒流角色战斗统计不准的问题。

---

## 新功能

### 导出本局数据

战绩详情页「AI 复盘」按钮下方新增「导出本局」按钮，点击后选择保存路径，把当前这一局打包成一个 zip，包内均为 JSON：

- `run.json` —— 本工具解析出的完整报告（角色、楼层事件时间线、卡组变化、遗物收获等），字段已中文本地化，方便直接阅读。
- `combat_logs/floor_XX.json` —— Pro Mode 采集到的逐场战斗日志（原始数据），没有安装 Pro Mode 或该局无战斗日志时不会有这部分文件。
- `run_source.json` —— 原始 `.run` 存档的原样拷贝（纯英文 / 原始 id），方便对照游戏或 mod 源码里的标识符查阅。

### 卡牌附魔解析

杀戮尖塔 2 的卡牌附魔系统（如「迅速」「锋利」，部分可叠加数值）此前完全没有解析。现在战绩详情页的楼层时间线会新增「本层附魔」分组，显示哪一层给哪张卡附了什么魔（及数值），AI 复盘提示词里也会带上这部分信息。

---

## 修复

### 毒 / 灾厄持续伤害在回合日志里显示为"（未知动作）"

战斗回合日志之前没识别毒 / 灾厄这类持续伤害动作，一律显示成"（未知动作）"——毒流角色（如静默猎人）的战斗日志会被刷屏。现在正确渲染成"☠ 中毒 · 伤害 [目标 -N]"。

### 单人局的毒伤害没有计入总伤害

之前不论单人还是联机，毒 / 灾厄伤害永远只进独立的"毒伤害"统计，不计入任何玩家的「总伤害」——联机局确实无法判断毒是谁上的，但单人局只有一个可能的施加者，本不该被排除。现改为：单人局毒伤害直接计入该玩家总伤害；联机局维持原样单独统计。此前毒流角色（尤其是静默猎人）的总伤害数据会明显偏低。

---

## 改进

- 调整了两个成就徽章的中文名（「连击大师」→「连连连连连段攻击」、「伤害王」→「伤害第一」）。

---

## 升级注意事项

- 从 v0.3.9 升级：直接覆盖安装即可，所有设置与数据保留。
- 升级后首次启动会因为解析缓存版本升级而重新解析全部历史战绩（卡牌附魔解析所需），仅发生一次，之后恢复正常速度。

---

## 反馈

测试版难免存在问题，欢迎通过 GitHub Issues 或邮件 `iryougi@iryougi.com` 反馈。
反馈战斗统计相关问题时，可一并附上 `安装目录/logs/` 下的日志文件，便于定位。
